% System
\RequirePackage{ifplatform}

% Graphics and colors
\RequirePackage[x11names, svgnames, dvipsnames]{xcolor}
\RequirePackage{tikz, tikzpagenodes}
\usetikzlibrary{patterns}

% Font
\RequirePackage{fontawesome5}
\RequirePackage{fontspec}

% Formatting
\RequirePackage[onehalfspacing]{setspace}
\RequirePackage{hyphenat}
\RequirePackage{array, parskip, adjustbox}
\RequirePackage[hidelinks]{hyperref}

% Set document font
\ifwindows
	\setmainfont[
		Extension	= .ttf,
		Ligatures	= TeX,
		BoldFont	= NotoSans-CondensedBold,
		ItalicFont	= NotoSans-CondensedItalic
	]{NotoSans-Condensed}
\else
	\setmainfont[
		Extension	= .ttf,
		Ligatures	= TeX,
		BoldFont	= NotoSans_Condensed-Bold,
		ItalicFont	= NotoSans_Condensed-Italic
	]{NotoSans_Condensed-Regular}
\fi

% No page numbers
\pagenumbering{gobble}
